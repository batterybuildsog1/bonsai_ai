# Bonsai AI Operator Memory

## Workspace Purpose

Operate the Bonsai AI BIM authoring tool across multiple building projects. Memory here is for operator knowledge -- prompt patterns, provider quirks, project-specific context.

## Active Projects

Track active projects in `reference/active-project.md`. Each project has its own directory under `projects/` with `context.md` and `log.md`.

## Operator Knowledge

(This section grows as the operator learns what works.)

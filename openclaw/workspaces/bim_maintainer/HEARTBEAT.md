# Heartbeat

Heartbeat is not the primary automation path for this workspace.

If heartbeat fires:

- check `reference/improvement-backlog.md` for any urgent items
- check if Bonsai_ai is now a git repo; if so, check for uncommitted changes
- do not start code changes without human approval
- otherwise reply `HEARTBEAT_OK`
